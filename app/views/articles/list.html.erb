<div class="container">
  <% @list.each do |element| %>
  <ul>
    <li>
      <p><%= link_to element.title, article_path(element) %></p>
      <p><%= element.content %></p>
    </li>
  </ul>
  <% end %>
</div>
