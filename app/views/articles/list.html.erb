<div class="container">
  <% @list.each do |element| %>
  <ul>
    <li>
      <p><%= link_to element.title, article_path(element) %></p>
      <p><%= element.content %></p>
    </li>
  </ul>
  <% end %>
  <br>
  <p><%= link_to 'Create new article', create_path %></p>
</div>
